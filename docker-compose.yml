version: '3.4'

services:
  rabbitmq:
    image: rabbitmq:3.8.5-alpine
    logging:
      driver: none

  celery:
    image: python:3.6.11-slim
    build: .
    depends_on:
      - rabbitmq
    volumes:
      - .:/app
